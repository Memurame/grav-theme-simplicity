title: text

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        inhalt:
          type: tab
          title: THEME_SIMPLICITY.GLOBAL.TABS.1
          fields:
            header.title:
              type: text
              autofocus: true
              label: THEME_SIMPLICITY.GLOBAL.FIELDS.TITLE.LABEL
            content:
              type: section
              title: "Content"
              underline: true
              fields:
                header.gallerie:
                  type: pagemedia
                  label: THEME_SIMPLICITY.PAGES.PROJECTITEM.FIELDS.SECTION.DETAIL.FIELDS.GALLERIE.LABEL
            settings:
              type: section
              title: "Einstellungen"
              underline: true
              fields:
                header.showtitle:
                  type: toggle
                  label: "Titel anzeigen?"
                  highlight: 1
                  default: 1
                  options:
                    0: "Nein"
                    1: "Ja"
                header.position:
                  type: toggle
                  label: THEME_SIMPLICITY.GLOBAL.FIELDS.POSITION.LABEL
                  highlight: main
                  default: main
                  options:
                    main: THEME_SIMPLICITY.GLOBAL.FIELDS.POSITION.OPTIONS.MAIN
                    sidebar: THEME_SIMPLICITY.GLOBAL.FIELDS.POSITION.OPTIONS.SIDEBAR
                  description: "Diese Einstellung ist nur auf der Home Seite verfügbar. Auf allen anderen hat diese keinen Einfluss"

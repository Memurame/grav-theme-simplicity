title: entry_list

form:
  fields:
    tabs:
      type: tabs
      active: 1
      fields:
        inhalt:
          type: tab
          title: THEME_SIMPLICITY.ADMIN.TAB.CONTENT
          fields:
            header.title:
              type: text
              autofocus: true
              label: THEME_SIMPLICITY.ADMIN.FIELDS.TITLE.LABEL
            header.reference:
              type: pages
              size: medium
              classes: fancy
              label: THEME_SIMPLICITY.ADMIN.FIELDS.REFERENCE.LABEL
              show_all: false
              show_modular: false
              show_root: false
              validate:
                required: true
            header.count:
              type: select
              label: THEME_SIMPLICITY.ADMIN.FIELDS.COUNT.LABEL
              size: medium
              options:
                2: THEME_SIMPLICITY.ADMIN.FIELDS.COUNT.OPTIONS.2
                5: THEME_SIMPLICITY.ADMIN.FIELDS.COUNT.OPTIONS.5
                10: THEME_SIMPLICITY.ADMIN.FIELDS.COUNT.OPTIONS.10
              validate:
                required: true
            header.layout:
              type: select
              label: THEME_SIMPLICITY.ADMIN.FIELDS.LAYOUT.LABEL
              size: medium
              options:
                1: THEME_SIMPLICITY.ADMIN.FIELDS.LAYOUT.OPTIONS.1
                2: THEME_SIMPLICITY.ADMIN.FIELDS.LAYOUT.OPTIONS.2
              validate:
                required: true
            header.showtags:
              type: toggle
              label: THEME_SIMPLICITY.ADMIN.FIELDS.SHOWTAGS.LABEL
              highlight: 1
              default: 1
              options:
                0: THEME_SIMPLICITY.ADMIN.FIELDS.SHOWTAGS.OPTIONS.0
                1: THEME_SIMPLICITY.ADMIN.FIELDS.SHOWTAGS.OPTIONS.1
            header.slice:
              type: toggle
              label: THEME_SIMPLICITY.ADMIN.FIELDS.SLICE.LABEL
              highlight: 0
              default: 0
              options:
                0: THEME_SIMPLICITY.ADMIN.FIELDS.SLICE.OPTIONS.0
                1: THEME_SIMPLICITY.ADMIN.FIELDS.SLICE.OPTIONS.1
            settings:
              type: section
              title: THEME_SIMPLICITY.ADMIN.SECTION.SETTINGS
              underline: true
              fields:
                header.showtitle:
                  type: toggle
                  label: THEME_SIMPLICITY.ADMIN.FIELDS.SHOWTITLE.LABEL
                  highlight: 1
                  default: 1
                  options:
                    0: THEME_SIMPLICITY.ADMIN.FIELDS.SHOWTITLE.OPTIONS.0
                    1: THEME_SIMPLICITY.ADMIN.FIELDS.SHOWTITLE.OPTIONS.1
                header.position:
                  type: toggle
                  label: THEME_SIMPLICITY.ADMIN.FIELDS.POSITION.LABEL
                  highlight: main
                  default: main
                  options:
                    main: THEME_SIMPLICITY.ADMIN.FIELDS.POSITION.OPTIONS.MAIN
                    sidebar: THEME_SIMPLICITY.ADMIN.FIELDS.POSITION.OPTIONS.SIDEBAR
                  description: THEME_SIMPLICITY.ADMIN.FIELDS.POSITION.DESCRIPTION


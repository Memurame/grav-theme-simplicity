title: text

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        inhalt:
          type: tab
          title: THEME_SIMPLICITY.GLOBAL.TABS.1
          fields:
            header.title:
              type: text
              autofocus: true
              label: THEME_SIMPLICITY.GLOBAL.FIELDS.TITLE.LABEL
            header.position:
              type: toggle
              label: THEME_SIMPLICITY.GLOBAL.FIELDS.POSITION.LABEL
              highlight: main
              default: main
              options:
                main: THEME_SIMPLICITY.GLOBAL.FIELDS.POSITION.OPTIONS.MAIN
                sidebar: THEME_SIMPLICITY.GLOBAL.FIELDS.POSITION.OPTIONS.SIDEBAR
            header.text:
              type: markdown
              label: THEME_SIMPLICITY.MODULAR.TEXT.FIELDS.TEXT.LABEL
              validate:
                type: textarea

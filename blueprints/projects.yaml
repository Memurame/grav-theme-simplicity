title: projects

form:
  fields:
    tabs:
      type: tabs
      active: 1

      fields:
        inhalt:
          type: tab
          title: THEME_SIMPLICITY.GLOBAL.TABS.1
          fields:
            header.title:
              type: text
              autofocus: true
              label: THEME_SIMPLICITY.GLOBAL.FIELDS.TITLE.LABEL

